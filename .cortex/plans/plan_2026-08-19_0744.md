---
created: 2026-08-19T07:44:29.681Z
session: 3fa128f9-047f-4d7f-a0f6-572c0b2c2a1a
working_directory: /Users/hroy/Desktop/claude-projects/coco-cli-streamlit-v1
---

## Sync plan: coco-agents/web-interface → coco-cli-streamlit-v1

**Context:** The external copy (`/Users/hroy/coco-agents/web-interface`) is a substantially rewritten version of this app: renamed entry point (`app.py` → `Agentic_Control_Tower.py`), pages consolidated from 6 down to 3 (`2_Dashboard.py`, `3_Ask_Data.py`, `4_How_It_Works.py`), a new persona system (`components/persona.py`, `config/personas.py`), a new `services/case_manager.py` (CRUD for cases/audit log), expanded `services/pipeline.py` (7-hop evidence traversal, JSON handling fixes) and `services/queries.py`/`snowflake_connection.py` (added query caching, refactored internals), new `assets/` images, `README.md`, `environment.yml`, and new `.streamlit/config.toml` + `.streamlit/pages.toml`.

### Step 1 — Copy Python source files directly (no confirmation needed)
- Delete old `app.py`, `pages/1_Procurement_Dashboard.py`, `2_Business_Events.py`, `3_AI_Reasoning_Agent.py`, `4_Full_Pipeline.py`, `5_How_It_Works.py`, `6_Pipeline_Audit.py` (superseded by the consolidated pages).
- Copy new/updated files verbatim: `Agentic_Control_Tower.py`, `pages/2_Dashboard.py`, `pages/3_Ask_Data.py`, `pages/4_How_It_Works.py`, `components/styles.py`, `components/persona.py` (new), `config/personas.py`, `config/settings.py`, `services/pipeline.py`, `services/queries.py`, `services/snowflake_connection.py`, `services/case_manager.py` (new). `services/actions.py` is identical — skip.
- Copy supporting non-config content: `assets/*.png`, `README.md` (new additions, not config).
- Remove stale `__pycache__` dirs (regenerated automatically).

### Step 2 — Configuration files: show diff/content and get explicit confirmation before writing
For each, I'll show you the exact change and wait for a yes before applying:
1. `snowflake.yml` — entity renamed `procurement_dashboard`→`procurement_control_tower`, identifier simplified, `main_file` changed to `Agentic_Control_Tower.py`, artifacts list updated, `stage:` key removed.
2. `.streamlit/secrets.toml` — **credentials differ**: current file uses `administrator`/`ACCOUNTADMIN`; source uses a scoped `STREAMLIT_APP_USER`/`STREAMLIT_APP_ROLE`. I will NOT overwrite this without you confirming which credentials should actually be used (this affects live DB access).
3. `.streamlit/config.toml` — new file (theme + server settings), doesn't exist in destination yet.
4. `.streamlit/pages.toml` — new file (custom page nav/titles), doesn't exist in destination yet.
5. `environment.yml` — new file (conda env spec), doesn't exist in destination yet.
- `requirements.txt`, `.devcontainer/devcontainer.json`, `.gitignore` are identical between the two — no action needed.

### Step 3 — Verify
- `grep`/`find` to confirm final directory structure matches source (minus caches/pyc).
- Spot-check imports in copied files resolve against the new file layout (e.g., `Agentic_Control_Tower.py` imports, `case_manager.py` imports from `services.snowflake_connection`).
- Optionally run `streamlit run Agentic_Control_Tower.py` briefly to confirm it boots (only if you want a live check).

**Critical files:**
- `Agentic_Control_Tower.py` — new main entry point replacing `app.py`
- `services/pipeline.py` — most heavily changed logic (detection/investigation pipeline)
- `services/snowflake_connection.py` — refactored connection/query internals used everywhere
- `.streamlit/secrets.toml` / `snowflake.yml` — config files needing your explicit sign-off before change
